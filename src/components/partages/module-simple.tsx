"use client";
import { useEffect, useState } from "react";

type Ligne=Record<string, unknown>;
export function ModuleSimple({module}:{module:string}){const [lignes,setLignes]=useState<Ligne[]>([]); const [chargement,setChargement]=useState(true); useEffect(()=>{fetch(`/api/${module}`,{cache:'no-store'}).then(r=>r.json()).then(x=>setLignes(x.donnees??[])).finally(()=>setChargement(false))},[module]); return <div className="rounded-2xl border bg-white shadow-sm"><div className="flex items-center justify-between border-b p-4"><p className="font-semibold">Liste enregistrée</p><span className="rounded-full bg-slate-100 px-3 py-1 text-xs">{lignes.length} élément(s)</span></div><div className="overflow-x-auto"><table className="w-full text-sm"><thead className="bg-slate-50 text-left"><tr><th className="p-3">Référence / Code</th><th className="p-3">Libellé principal</th><th className="p-3">État</th></tr></thead><tbody>{chargement?<tr><td className="p-6 text-center" colSpan={3}>Chargement...</td></tr>:lignes.length===0?<tr><td className="p-6 text-center text-slate-500" colSpan={3}>Aucune donnée.</td></tr>:lignes.map((l,i)=><tr key={i} className="border-t"><td className="p-3">{String(l.reference??l.code??l.matricule??l.email??l.id??'—')}</td><td className="p-3">{String(l.titre??l.nom??l.nomComplet??l.etudiant??'—')}</td><td className="p-3">{String(l.statut??(l.estActif===false?'Inactif':'Actif'))}</td></tr>)}</tbody></table></div></div>}
