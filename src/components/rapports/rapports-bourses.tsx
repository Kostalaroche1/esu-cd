"use client";
import { useEffect,useState } from "react";
import { Printer } from "lucide-react";
import { Carte } from "@/components/ui/carte";
type Rapport={total:number;retenues:number;rejetees:number;actives:number;montantPaye:number;recentes:Array<any>};
export function RapportsBourses(){const [r,setR]=useState<Rapport|null>(null);useEffect(()=>{fetch('/api/rapports',{cache:'no-store'}).then(x=>x.json()).then(x=>setR(x.donnees))},[]);if(!r)return <p>Chargement...</p>;return <div className="space-y-6"><div className="flex justify-end"><button onClick={()=>window.print()} className="flex items-center gap-2 rounded-lg bg-slate-950 px-4 py-2 text-sm font-semibold text-white"><Printer className="size-4"/>Imprimer / PDF</button></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-5"><Carte titre="Candidatures" valeur={r.total}/><Carte titre="Retenues" valeur={r.retenues}/><Carte titre="Rejetées" valeur={r.rejetees}/><Carte titre="Bourses actives" valeur={r.actives}/><Carte titre="Montant payé" valeur={`${r.montantPaye.toLocaleString('fr-FR')} USD`}/></div><div className="rounded-2xl border bg-white"><div className="border-b p-4 font-semibold">Candidatures récentes</div><div className="overflow-x-auto"><table className="w-full text-sm"><thead><tr className="bg-slate-50 text-left"><th className="p-3">Référence</th><th className="p-3">Étudiant</th><th className="p-3">Appel</th><th className="p-3">Statut</th></tr></thead><tbody>{r.recentes.map(c=><tr className="border-t" key={c.id}><td className="p-3">{c.reference}</td><td className="p-3">{c.etudiant.prenom} {c.etudiant.nom}</td><td className="p-3">{c.appel.titre}</td><td className="p-3">{c.statut}</td></tr>)}</tbody></table></div></div></div>}
